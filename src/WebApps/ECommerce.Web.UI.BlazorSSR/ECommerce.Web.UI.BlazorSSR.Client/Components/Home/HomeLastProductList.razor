@using ECommerce.Web.UI.BlazorSSR.Client.Components.Products
@rendermode InteractiveAuto

<MudPaper>
    <MudExpansionPanels>
        <MudExpansionPanel Expanded="true" Style="@($"background:{Colors.Gray.Darken4};padding:0 0 0 0 !important;")">
            <TitleContent>
                <MudIcon Icon="@Icons.Material.Filled.Star" />
                Last Products
            </TitleContent>
            <ChildContent>
                @if (!products.Any())
                {
                    <span> Loading Products...</span>
                }
                else
                {
                    <div style="display: flex; overflow-x: auto; white-space: nowrap; padding: 1rem;">
                        @foreach (var product in products)
                        {
                            <ProductCard Product="product"/>
                        }
                    </div>
                }
            </ChildContent>
        </MudExpansionPanel>
    </MudExpansionPanels>
</MudPaper>
